; Factorio headless server config
; Uses separate write-data to avoid lock conflict with Steam client

[path]
write-data=/Users/mark/etc/claude/factorioctl/.factorio-server-data

[other]
check-updates=false
