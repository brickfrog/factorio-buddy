# Optional: live telemetry relay
RELAY_URL=
RELAY_TOKEN=

# Optional: Factorio mods directory (for --sync-mod)
# Auto-detected if not set (checks ~/.factorio/mods, Steam paths, etc.)
#FACTORIO_MODS_DIR=~/.factorio/mods
